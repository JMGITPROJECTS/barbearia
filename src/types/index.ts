export interface Usuario {
    nome: string;
    email: string;
    senha: string;
}

export interface Cliente {
    id: number;
    nome: string;
    cpf: string;
    telefone: string;
    email: string;
}